Show interface relative to their dns servers:
resolvectl status

Show to through what interface dns requests go:
ip route get <dns_ip>

To resolve dns you can use either dig or nslookup:
dig <url> @<dns_server_ip> +short
---
nslookup <url>
