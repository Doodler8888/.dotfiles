Print out current partitions with their fs types:
df -Th | grep "^/dev"

Copy files between harddrives:
sudo mkdir /mnt/otherdrive
sudo mount /dev/sdb1 /mnt/otherdrive
cp -r /mnt/otherdrive/path/to/your/directory ~/
