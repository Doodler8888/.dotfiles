Update package:
sudo snap refresh <package_name>

List all updatable packages:
snap refresh --list

Update all packages (if it doesn't update, that means that target packages are
opened currently):
sudo snap refresh
