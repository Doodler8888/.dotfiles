Source config:
tmux-source /path/to/config/file

Create a new tab:
<prefix> + c

Kill a tab:
<prefix> + &

Create a vertical split:
<prefix> + %

Create a horizontal split:
<prefix> + "

Kill a split:
<prefix> + x

Cycle between splits:
<prefix> + o

Move to a split:
<prefix> + <arrow>
