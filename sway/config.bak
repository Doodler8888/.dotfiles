# # Modifier key
# set $mod Mod4

# # Application launching
# bindsym $mod+Return exec alacritty
# bindsym $mod+Shift+b exec opera
# bindsym $mod+b exec vivaldi-stable
# bindsym $mod+m exec dolphin
# bindsym $mod+r exec wofi
# bindsym $mod+e exec emacs

# # Workspace management
# bindsym $mod+1 workspace 1
# bindsym $mod+2 workspace 2

# # Exit Sway
# bindsym $mod+q exec swaymsg exit

# set $ws1 "browsers"
# set $ws2 "emacs"

# workspace $ws2 {
#     layout tabbed
#     exec emacsclient -c -a "" --eval "(load-desktop-session \"main\")" -F '(name . "project1")'
#     exec emacsclient -c -a "" --eval "(load-desktop-session \"shelf\")" -F '(name . "project2")'
# }

# for_window [app_id="emacs" title="project1"] {
#     exec alacritty --title "terminal-project1"
# }

# for_window [app_id="emacs" title="project2"] {
#     exec alacritty --title "terminal-project2"
# }

# bindsym $mod+t [app_id="emacs"] focus
# bindsym $mod+Shift+t [title="^terminal-"] focuss